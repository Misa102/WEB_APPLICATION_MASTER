import { makeStyles } from '@material-ui/core/styles';


export default makeStyles((them)=>({
    fab: {
        position:'fixed',
        bottom: them.spacing(2),
        right: them.spacing(2),
    }
}))